// Buscar la línea que dice:
// body: JSON.stringify({

// Y reemplazarla por:
// body: JSON.stringify({
//     action: 'register',

// Para el login:
// body: JSON.stringify({
//     action: 'login',
